In order to run, Python3 is required

In addition, you will need to install the Python mysql connector and numpy for addTestData.py
and mysql connector for hashAllPasswords.py.
These can be installed by running the following pip commands in the command line:
    'pip install mysql-connector-python'
    'pip install numpy'
    'pip install bcrypt'